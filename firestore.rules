
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEBUG RULES - COMPLETELY OPEN FOR TESTING
    // TODO: Replace with secure rules once permissions issue is resolved
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
